package xmp.objects;

/**
 * ClickableObject, jonka paikka voidaan vaihtaa toisen SwappableObjectin kanssa.
 * @see xmp.objects.ClickableObject
 */
public class SwappableObject extends ClickableObject{
    
    public SwappableObject(int x, int y, int w, int h) {
        super(x, y, w, h);
    }
    
}
